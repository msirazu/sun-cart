import RegisterForm from "@/features/auth/RegisterForm";

export const metadata = {
    title: 'Register | Sun Cart'
}

const RegisterPage = () => {
    return (
        <div>
            <RegisterForm/>
        </div>
    );
};

export default RegisterPage;