import LoginForm from "@/features/auth/LoginForm";

export const metadata = {
    title: 'Login | Sun Cart'
}

const LoginPage = () => {
    return (
        <div>
            <LoginForm/>
        </div>
    );
};

export default LoginPage;