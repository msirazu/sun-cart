import LoadingColors from "@/components/ui/loader/LoadingColors";

const GlobalLoading = () => {
    return (
        <div>
        <LoadingColors/>
        </div>
    );
};

export default GlobalLoading;